{
  "name": "dev-sync",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "build:server": "npm run build --workspace=packages/types && npm run build --workspace=@dev-sync/server",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check",
    "db:generate": "cd apps/server && npx prisma generate --schema=./prisma/schema.prisma",
    "db:push": "cd apps/server && npx prisma db push --schema=./prisma/schema.prisma",
    "db:migrate": "cd apps/server && npx prisma migrate dev --schema=./prisma/schema.prisma",
    "db:studio": "cd apps/server && npx prisma studio --schema=./prisma/schema.prisma"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.4.0",
    "prettier": "^3.2.0"
  },
  "engines": {
    "node": ">=20"
  }
}
